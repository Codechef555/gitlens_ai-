{"repository":{"id":"367bb82c-6f31-4e9e-bfe2-36f833d9aaec","name":"Intelligent_surveillance_system","source":"github","url":"https://github.com/Codechef555/Intelligent_surveillance_system","fileCount":29,"primaryLanguage":"Python","defaultBranch":"main","sizeLabel":"29 files"},"analysis":{"fileTree":["car_crash_pt/best.pt","car_crash_pt/coco1.txt","Fall_detection_pt/classes.txt","Fall_detection_pt/fall.mp4","Fall_detection_pt/fall_detection_outputs/fall_detection_output.mp4","Fall_detection_pt/main.py","Fall_detection_pt/mainy.py","Fall_detection_pt/requirements.txt","Fall_detection_pt/yolov8s.pt","home.py","pages/car_crash_detection.py","pages/fall_detection.py","pages/violence.py","pages/vit_transformer_module.py","pages/weapon_de.py","pages/weapon_detection.py","pages/__pycache__/vit_transformer_module.cpython-311.pyc","Project_documentation.pdf","Published_conference_paper.pdf","readme.md","test_set/output/o1.jpg","test_set/output/o2.jpg","test_set/output/o3.jpg","test_set/output/o4.jpg","utils/vit_transformer_module.py","utils/__pycache__/vit_transformer_module.cpython-311.pyc","violence_pt/best.pt","violence_pt/coco.txt","violence_pt/pose.pt"],"languages":[{"name":"Python","files":10}],"frameworks":["FastAPI"],"modules":[{"kind":"shared","name":"Shared building blocks","files":["utils/vit_transformer_module.py","utils/__pycache__/vit_transformer_module.cpython-311.pyc"]}],"dependencies":[],"circularDependencies":[],"architecture":{"nodes":[{"id":"shared","label":"Shared building blocks","kind":"shared","files":["utils/vit_transformer_module.py","utils/__pycache__/vit_transformer_module.cpython-311.pyc"]}],"edges":[]},"summary":"29 files across Python, using FastAPI. Key areas include shared building blocks."},"files":[{"path":"car_crash_pt/best.pt","language":"Other","size":22512291},{"path":"car_crash_pt/coco1.txt","language":"Other","size":18},{"path":"Fall_detection_pt/classes.txt","language":"Other","size":624},{"path":"Fall_detection_pt/fall.mp4","language":"Other","size":1403572},{"path":"Fall_detection_pt/fall_detection_outputs/fall_detection_output.mp4","language":"Other","size":6033912},{"path":"Fall_detection_pt/main.py","language":"Python","size":1456,"preview":"import cv2\nimport cvzone\nimport math\nfrom ultralytics import YOLO\n\ncap = cv2.VideoCapture('fall.mp4')\n\nmodel = YOLO('yolov8s.pt')\n\nclassnames = []\nwith open('classes.txt', 'r') as f:\n    classnames = f.read().splitlines()\n\n\nwhile True:\n    ret, frame = cap.read()\n    frame = cv2.resize(frame, (980,740))\n\n    results = model(frame)\n\n    for info in results:\n        parameters = info.boxes\n        for box in parameters:\n            x1, y1, x2, y2 = box.xyxy[0]\n            x1, y1, x2, y2 = int(x1), int(y1), int(x2), int(y2)\n            confidence = box.conf[0]\n            class_detect = box.cls[0]\n            class_detect = int(class_detect)\n            class_detect = classnames[class_detect]\n            conf = math.ceil(confidence * 100)\n\n\n            # implement fall detection using the coordinates x1,y1,x2\n            height = y2 - y1\n            width = x2 - x1\n            threshold  = height - width\n\n            if conf > 80 and class_detect == 'person':\n                cvzone.cornerRect(frame, [x1, y1, width, height], l=30, rt=6)\n                cvzone.putTextRect(frame, f'{class_detect}', [x1 + 8, y1 - 12], thickness=2, scale=2)\n            \n            if threshold < 0:\n                cvzone.putTextRect(frame, 'Fall Detected', [height, width], thickness=2, scale=2)\n            \n            else:pass\n\n\n    cv2.imshow('frame', frame)\n    if cv2.waitKey(1) & 0xFF == ord('t'):\n        break\n\n\ncap.release()\ncv2.destroyAllWindows()\n"},{"path":"Fall_detection_pt/mainy.py","language":"Python","size":2046,"preview":"import cv2\nimport cvzone\nimport math\nimport os\nfrom ultralytics import YOLO\n\n# Load video and YOLO model\ncap = cv2.VideoCapture('fall.mp4')\nmodel = YOLO('yolov8s.pt')\n\n# Load class names\nwith open('classes.txt', 'r') as f:\n    classnames = f.read().splitlines()\n\n# Define frame size\nframe_width = 980\nframe_height = 740\n\n# Setup output folder and writer\noutput_folder = \"fall_detection_outputs\"\nos.makedirs(output_folder, exist_ok=True)\noutput_path = os.path.join(output_folder, \"fall_detection_output.mp4\")\n\n# Define codec and create VideoWriter\nfps = int(cap.get(cv2.CAP_PROP_FPS))\nfourcc = cv2.VideoWriter_fourcc(*'mp4v')\nout = cv2.VideoWriter(output_path, fourcc, fps, (frame_width, frame_height))\n\nwhile True:\n    ret, frame = cap.read()\n    if not ret:\n        break  # End of video\n    \n    frame = cv2.resize(frame, (frame_width, frame_height))\n    results = model(frame)\n\n    for info in results:\n        parameters = info.boxes\n        for box in parameters:\n            x1, y1, x2, y2 = box.xyxy[0]\n            x1, y1, x2, y2 = int(x1), int(y1), int(x2), int(y2)\n            confidence = box.conf[0]\n            class_detect = int(box.cls[0])\n            class_name = classnames[class_detect]\n            conf = math.ceil(confidence * 100)\n\n            height = y2 - y1\n            width = x2 - x1\n            threshold = height - width\n\n            if conf > 80 and class_name == 'person':\n                cvzone.cornerRect(frame, [x1, y1, width, height], l=30, rt=6)\n                cvzone.putTextRect(frame, f'{class_name}', [x1 + 8, y1 - 12], thickness=2, scale=2)\n                \n                if threshold < 0:\n                    cvzone.putTextRect(frame, 'Fall Detected', [x1, y1 + height + 20], thickness=2, scale=2)\n\n    # Show and save the processed frame\n    cv2.imshow('frame', frame)\n    out.write(frame)\n\n    if cv2.waitKey(1) & 0xFF == ord('t'):  # Press 't' to break\n        break\n\n# Release resources\ncap.release()\nout.release()\ncv2.destroyAllWindows()\n\nprint(f\"✅ Fall detection output saved at: {output_path}\")\n"},{"path":"Fall_detection_pt/requirements.txt","language":"Other","size":18},{"path":"Fall_detection_pt/yolov8s.pt","language":"Other","size":22573363},{"path":"home.py","language":"Python","size":1612,"preview":"import streamlit as st\nfrom PIL import Image\nfrom streamlit_lottie import st_lottie\nimport json\n\nst.set_page_config(page_title=\"Intelligent Surveillance System\", layout=\"wide\")\nst.title(\"📷 Intelligent Surveillance System\")\n\nst.subheader(\"🛡️ Panel A: Suspicious Activity Detection\")\n\ncol1, col2, col3 = st.columns(3)\n\nwith col1:\n    st.markdown(\"### 🔥 Violence Detection\")\n    st.markdown(\"- Detects **Violent**, **Non-Violent**, and **Stabbing** scenes.\")\n    if st.button(\"Go to Violence Detection\"):\n        st.switch_page(\"pages/violence.py\")  \n\nwith col2:\n    st.markdown(\"### 🚗 Car Crash Detection\")\n    st.markdown(\"- Detects sudden **car crash incidents** in footage.\")\n    if st.button(\"Go to Car Crash Detection\"):\n        st.switch_page(\"pages/car_crash_detection.py\")  \n\nwith col3:\n    st.markdown(\"### 🤕 Fall Detection\")\n    st.markdown(\"- Detects **human falls** for emergency response.\")\n    if st.button(\"Go to Fall Detection\"):\n        st.switch_page(\"pages/fall_detection.py\")  \n\nst.divider()\n\nst.subheader(\"🔍 Panel B: Weapon Involvement Detection\")\n\nst.markdown(\"\"\"\nUpload a **video or image** file to analyze for potential **weapon involvement**. The system will detect and classify weapons, providing a **detailed summary** of findings.\n\"\"\")\n\nuploaded_file = st.file_uploader(\"Upload a surveillance image/video file\", type=[\"jpg\", \"png\", \"mp4\", \"avi\"])\n\nif uploaded_file is not None:\n    st.success(\"File successfully uploaded!\")\n\nst.markdown(\"##### 🎯 Analyze Video/Image for Weapons\")\n\nif st.button(\"Go to Weapon Detection\"):\n    st.switch_page(\"pages/weapon_de.py\")\n\n"},{"path":"pages/car_crash_detection.py","language":"Python","size":2009,"preview":"import streamlit as st\nimport cv2\nimport pandas as pd\nfrom ultralytics import YOLO\nimport cvzone\nfrom PIL import Image\nimport tempfile\nimport os\n\n# Set page title\nst.set_page_config(page_title=\"Car Crash Detection\", layout=\"wide\")\n\nst.title(\"🚗 Car Crash Detection System\")\n\n# Load model\n@st.cache_resource\ndef load_model():\n    return YOLO('C:/Users/gawra/Downloads/code-upgrade/streamlit/car_crash_pt/best.pt')\n\nmodel = load_model()\n\n# Load class names\n@st.cache_data\ndef load_class_list():\n    with open(\"C:/Users/gawra/Downloads/code-upgrade/streamlit/car_crash_pt/coco1.txt\", \"r\") as f:\n        return f.read().split(\"\\n\")\n\nclass_list = load_class_list()\n\n# Upload video\nuploaded_video = st.file_uploader(\"Upload a video\", type=[\"mp4\", \"avi\", \"mov\"])\n\nif uploaded_video is not None:\n    tfile = tempfile.NamedTemporaryFile(delete=False) \n    tfile.write(uploaded_video.read())\n    cap = cv2.VideoCapture(tfile.name)\n\n    stframe = st.empty()\n    count = 0\n\n    while cap.isOpened():\n        ret, frame = cap.read()\n        if not ret:\n            break\n\n        count += 1\n        if count % 3 != 0:\n            continue\n\n        frame = cv2.resize(frame, (1020, 500))\n        results = model.predict(frame)\n        detections = results[0].boxes.data\n        px = pd.DataFrame(detections).astype(\"float\")\n\n        for _, row in px.iterrows():\n            x1, y1, x2, y2 = int(row[0]), int(row[1]), int(row[2]), int(row[3])\n            class_id = int(row[5])\n            class_name = class_list[class_id]\n\n            color = (0, 0, 255) if 'accident' in class_name else (0, 255, 0)\n            cv2.rectangle(frame, (x1, y1), (x2, y2), color, 2)\n            cvzone.putTextRect(frame, f'{class_name}', (x1, y1), 1, 1)\n\n        # Convert BGR to RGB\n        frame_rgb = cv2.cvtColor(frame, cv2.COLOR_BGR2RGB)\n        stframe.image(frame_rgb, channels=\"RGB\", use_column_width=True)\n\n    cap.release()\n    os.unlink(tfile.name)\n\nelse:\n    st.info(\"Please upload a video file to begin car crash detection.\")\n\n"},{"path":"pages/fall_detection.py","language":"Python","size":2685,"preview":"import streamlit as st\nimport cv2\nimport cvzone\nimport math\nfrom ultralytics import YOLO\nfrom PIL import Image\nimport tempfile\nimport os\n\n# Set page config\nst.set_page_config(page_title=\"Fall Detection System\", layout=\"wide\")\nst.title(\"🧍 Fall Detection System\")\n\n# Load model with caching\n@st.cache_resource\ndef load_model():\n    return YOLO('Fall_detection_pt/yolov8s.pt')\n\nmodel = load_model()\n\n# Load class names\n@st.cache_data\ndef load_classes():\n    with open('Fall_detection_pt/classes.txt', 'r') as f:\n        return f.read().splitlines()\n\nclassnames = load_classes()\n\n# Upload video file\nuploaded_video = st.file_uploader(\"📹 Upload a video\", type=[\"mp4\", \"avi\", \"mov\"])\n\nif uploaded_video is not None:\n    tfile = tempfile.NamedTemporaryFile(delete=False)\n    tfile.write(uploaded_video.read())\n    cap = cv2.VideoCapture(tfile.name)\n\n    stframe = st.empty()\n\n    while cap.isOpened():\n        ret, frame = cap.read()\n        if not ret:\n            break\n\n        frame = cv2.resize(frame, (980, 740))\n        results = model(frame)\n\n        for info in results:\n            parameters = info.boxes\n            for box in parameters:\n                x1, y1, x2, y2 = box.xyxy[0]\n                x1, y1, x2, y2 = int(x1), int(y1), int(x2), int(y2)\n                confidence = box.conf[0]\n                class_detect = box.cls[0]\n                class_detect = int(class_detect)\n                class_detect = classnames[class_detect]\n                conf = math.ceil(confidence * 100)\n\n                height = y2 - y1\n                width = x2 - x1\n                threshold = height - width\n\n                if conf > 80 and class_detect == 'person':\n                    if threshold < 0:\n                        # RED bounding box for fall\n                        cv2.rectangle(frame, (x1, y1), (x2, y2), (0, 0, 255), thickness=4)\n                        # Bold, big RED text\n                        cv2.putText(frame, 'FALL DETECTED', (x1, y2 + 40),\n                                    cv2.FONT_HERSHEY_SIMPLEX, 1.5, (0, 0, 255), thickness=4, lineType=cv2.LINE_AA)\n                    else:\n                        # Normal person detection\n                        cvzone.cornerRect(frame, [x1, y1, width, height], l=30, rt=6)\n                        cvzone.putTextRect(frame, f'{class_detect}', [x1 + 8, y1 - 12], thickness=2, scale=2)\n\n        # Convert BGR to RGB for Streamlit\n        frame_rgb = cv2.cvtColor(frame, cv2.COLOR_BGR2RGB)\n        stframe.image(frame_rgb, channels=\"RGB\", use_column_width=True)\n\n    cap.release()\n    os.unlink(tfile.name)\n\nelse:\n    st.info(\"⬆️ Please upload a video to start fall detection.\")\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n"},{"path":"pages/violence.py","language":"Python","size":1698,"preview":"import streamlit as st\nimport cv2\nimport numpy as np\nimport pandas as pd\nfrom ultralytics import YOLO\nimport tempfile\nimport cvzone\n\n\n# Load model and class names\nmodel = YOLO('violence_pt/best.pt')  # Make sure this path is correct\nwith open(\"violence_pt/coco.txt\", \"r\") as f:\n    class_list = f.read().splitlines()\n\n# Streamlit interface\nst.title(\"🔍 Violence & Suspicious activity detection\")\n\nuploaded_file = st.file_uploader(\"Upload a video\", type=[\"mp4\", \"avi\", \"mov\", \"mkv\"])\n\ndef process_frame(frame):\n    frame = cv2.resize(frame, (1020, 500))\n    results = model.predict(frame)\n    boxes = results[0].boxes.data\n    px = pd.DataFrame(boxes).astype(\"float\")\n\n    for _, row in px.iterrows():\n        x1, y1, x2, y2 = map(int, row[:4])\n        class_id = int(row[5])\n        label = class_list[class_id]\n\n        if 'stabbing' in label:\n            color = (0, 0, 255)  # Red\n        elif 'violence' in label:\n            color = (255, 0, 255)  # Purple\n        else:\n            color = (0, 255, 0)  # Green\n\n        cv2.rectangle(frame, (x1, y1), (x2, y2), color, 2)\n        cvzone.putTextRect(frame, f'{label}', (x1, y1), scale=2, thickness=2, colorB=color)\n\n    return frame\n\n# Process video if uploaded\nif uploaded_file:\n    tfile = tempfile.NamedTemporaryFile(delete=False)\n    tfile.write(uploaded_file.read())\n\n    cap = cv2.VideoCapture(tfile.name)\n    stframe = st.empty()\n\n    while cap.isOpened():\n        ret, frame = cap.read()\n        if not ret:\n            break\n        processed = process_frame(frame)\n        stframe.image(cv2.cvtColor(processed, cv2.COLOR_BGR2RGB), channels=\"RGB\", use_column_width=True)\n\n    cap.release()\n    st.success(\"✅ Processing complete!\")\n"},{"path":"pages/vit_transformer_module.py","language":"Python","size":10971,"preview":"import torch\nimport torchvision\n\nfrom torch import nn\nfrom torchvision import transforms\n\n\n# 1. Create a class which subclasses nn.Module\nclass PatchEmbedding(nn.Module):\n    \"\"\"Turns a 2D input image into a 1D sequence learnable embedding vector.\n    \n    Args:\n        in_channels (int): Number of color channels for the input images. Defaults to 3.\n        patch_size (int): Size of patches to convert input image into. Defaults to 16.\n        embedding_dim (int): Size of embedding to turn image into. Defaults to 768.\n    \"\"\" \n    # 2. Initialize the class with appropriate variables\n    def __init__(self, \n                 in_channels:int=3,\n                 patch_size:int=16,\n                 embedding_dim:int=768):\n        super().__init__()\n        patch_size=16\n        # 3. Create a layer to turn an image into patches\n        self.patcher = nn.Conv2d(in_channels=in_channels,\n                                 out_channels=embedding_dim,\n                                 kernel_size=patch_size,\n                                 stride=patch_size,\n                                 padding=0)\n\n        # 4. Create a layer to flatten the patch feature maps into a single dimension\n        self.flatten = nn.Flatten(start_dim=2, # only flatten the feature map dimensions into a single vector\n                                  end_dim=3)\n\n    # 5. Define the forward method \n    def forward(self, x):\n        # Create assertion to check that inputs are the correct shape\n        patch_size=16\n        image_resolution = x.shape[-1]\n        assert image_resolution % patch_size == 0, f\"Input image size must be divisble by patch size, image shape: {image_resolution}, patch size: {patch_size}\"\n        \n        # Perform the forward pass\n        x_patched = self.patcher(x)\n        x_flattened = self.flatten(x_patched) \n        \n        # 6. Make sure the output shape has the right order \n        return x_flattened.permute(0, 2, 1) # adjust so the embedding is on the final dimension [batch_size, P^2•C, N] -> [batch_size, N, P^2•C]\n        \n        \n    \n# 1. Create a class that inherits from nn.Module\nclass MultiheadSelfAttentionBlock(nn.Module):\n    \"\"\"Creates a multi-head self-attention block (\"MSA block\" for short).\n    \"\"\"\n    # 2. Initialize the class with hyperparameters from Table 1\n    def __init__(self,\n                 embedding_dim:int=768, # Hidden size D from Table 1 for ViT-Base\n                 num_heads:int=12, # Heads from Table 1 for ViT-Base\n                 attn_dropout:float=0): # doesn't look like the paper uses any dropout in MSABlocks\n        super().__init__()\n        \n        # 3. Create the Norm layer (LN)\n        self.layer_norm = nn.LayerNorm(normalized_shape=embedding_dim)\n        \n        # 4. Create the Multi-Head Attention (MSA) layer\n        self.multihead_attn = nn.MultiheadAttention(embed_dim=embedding_dim,\n                                                    num_heads=num_heads,\n                                                    dropout=attn_dropout,\n                                                    batch_first=True) # does our batch dimension come first?\n        \n    # 5. Create a forward() method to pass the data throguh the layers\n    def forward(self, x):\n        x = self.layer_norm(x)\n        attn_output, _ = self.multihead_attn(query=x, # query embeddings \n                                             key=x, # key embeddings\n                                             value=x, # value embeddings\n                                             need_weights=False) # do we need the weights or just the layer outputs?\n        return attn_output\n        \n\n# 1. Create a class that inherits from nn.Module\nclass MLPBlock(nn.Module):\n    \"\"\"Creates a layer normalized multilayer perceptron block (\"MLP block\" for short).\"\"\"\n    # 2. Initialize the class with hyperparameters from Table 1 and Table 3\n    def __init__(self,\n                 embedding_dim:int=768, # Hidden Size D from Table 1 for ViT-Base\n                 mlp_size:int=3072, # MLP size from Table 1 for ViT-Base\n                 dropout:float=0.1): # Dropout from Table 3 for ViT-Base\n        super().__init__()\n        \n        # 3. Create the Norm layer (LN)\n        self.layer_norm = nn.LayerNorm(normalized_shape=embedding_dim)\n        \n        # 4. Create the Multilayer perceptron (MLP) layer(s)\n        self.mlp = nn.Sequential(\n            nn.Linear(in_features=embedding_dim,\n                      out_features=mlp_size),\n            nn.GELU(), # \"The MLP contains two layers with a GELU non-linearity (section 3.1).\"\n            nn.Dropout(p=dropout),\n            nn.Linear(in_features=mlp_size, # needs to take same in_features as out_features of layer above\n                      out_features=embedding_dim), # take back to embedding_dim\n            nn.Dropout(p=dropout) # \"Dropout, when used, is applied after every dense layer..\"\n        )\n    \n    # 5. Create a forward() method to pass the data throguh the layers\n    def forward(self, x):\n        x = self.layer_norm(x)\n        x = self.mlp(x)\n        return x\n        \n\n# 1. Create a class that inherits from nn.Module\nclass TransformerEncoderBlock(nn.Module):\n    \"\"\"Creates a Transformer Encoder block.\"\"\"\n    # 2. Initialize the class with hyperparameters from Table 1 and Table 3\n    def __init__(self,\n                 embedding_dim:int=768, # Hidden size D from Table 1 for ViT-Base\n                 num_heads:int=12, # Heads from Table 1 for ViT-Base\n                 mlp_size:int=3072, # MLP size from Table 1 for ViT-Base\n                 mlp_dropout:float=0.1, # Amount of dropout for dense layers from Table 3 for ViT-Base\n                 attn_dropout:float=0): # Amount of dropout for attention layers\n        super().__init__()\n\n        # 3. Create MSA block (equation 2)\n        self.msa_block = MultiheadSelfAttentionBlock(embedding_dim=embedding_dim,\n                                                     num_heads=num_heads,\n                                                     attn_dropout=attn_dropout)\n        \n        # 4. Create MLP block (equation 3)\n        self.mlp_block =  MLPBlock(embedding_dim=embedding_dim,\n                                   mlp_size=mlp_size,\n                                   dropout=mlp_dropout)\n        \n    # 5. Create a forward() method  \n    def forward(self, x):\n        \n        # 6. Create residual connection for MSA block (add the input to the output)\n        x =  self.msa_block(x) + x \n        \n        # 7. Create residual connection for MLP block (add the input to the output)\n        x = self.mlp_block(x) + x \n        \n        return x\n\n# 1. Create a ViT class that inherits from nn.Module\nclass ViT(nn.Module):\n    \"\"\"Creates a Vision Transformer architecture with ViT-Base hyperparameters by default.\"\"\"\n    # 2. Initialize the class with hyperparameters from Table 1 and Table 3\n    def __init__(self,\n                 img_size:int=224, # Training resolution from Table 3 in ViT paper\n                 in_channels:int=3, # Number of channels in input image\n                 patch_size:int=16, # Patch size\n                 num_transformer_layers:int=12, # Layers from Table 1 for ViT-Base\n                 embedding_dim:int=768, # Hidden size D from Table 1 for ViT-Base\n                 mlp_size:int=3072, # MLP size from Table 1 for ViT-Base\n                 num_heads:int=12, # Heads from Table 1 for ViT-Base\n                 attn_dropout:float=0, # Dropout for attention projection\n                 mlp_dropout:float=0.1, # Dropout for dense/MLP layers \n                 embedding_dropout:float=0.1, # Dropout for patch and position embeddings\n                 num_classes:int=1000): # Default for ImageNet but can customize this\n        super().__init__() # don't forget the super().__init__()!\n        \n        # 3. Make the image size is divisble by the patch size \n        assert img_size % patch_size == 0, f\"Image size must be divisible by patch size, image size: {img_size}, patch size: {patch_size}.\"\n        \n        # 4. Calculate number of patches (height * width/patch^2)\n        self.num_patches = (img_size * img_size) // patch_size**2\n                 \n        # 5. Create learnable class embedding (needs to go at front of sequence of patch embeddings)\n        self.class_embedding = nn.Parameter(data=torch.randn(1, 1, embedding_dim),\n                                            requires_grad=True)\n        \n        # 6. Create learnable position embedding\n        self.position_embedding = nn.Parameter(data=torch.randn(1, self.num_patches+1, embedding_dim),\n                                               requires_grad=True)\n                \n        # 7. Create embedding dropout value\n        self.embedding_dropout = nn.Dropout(p=embedding_dropout)\n        \n        # 8. Create patch embedding layer\n        self.patch_embedding = PatchEmbedding(in_channels=in_channels,\n                                              patch_size=patch_size,\n                                              embedding_dim=embedding_dim)\n        \n        # 9. Create Transformer Encoder blocks (we can stack Transformer Encoder blocks using nn.Sequential()) \n        # Note: The \"*\" means \"all\"\n        self.transformer_encoder = nn.Sequential(*[TransformerEncoderBlock(embedding_dim=embedding_dim,\n                                                                            num_heads=num_heads,\n                                                                            mlp_size=mlp_size,\n                                                                            mlp_dropout=mlp_dropout) for _ in range(num_transformer_layers)])\n       \n        # 10. Create classifier head\n        self.classifier = nn.Sequential(\n            nn.LayerNorm(normalized_shape=embedding_dim),\n            nn.Linear(in_features=embedding_dim, \n                      out_features=num_classes)\n        )\n    \n    # 11. Create a forward() method\n    def forward(self, x):\n        \n        # 12. Get batch size\n        batch_size = x.shape[0]\n        \n        # 13. Create class token embedding and expand it to match the batch size (equation 1)\n        class_token = self.class_embedding.expand(batch_size, -1, -1) # \"-1\" means to infer the dimension (try this line on its own)\n\n        # 14. Create patch embedding (equation 1)\n        x = self.patch_embedding(x)\n\n        # 15. Concat class embedding and patch embedding (equation 1)\n        x = torch.cat((class_token, x), dim=1)\n\n        # 16. Add position embedding to patch embedding (equation 1) \n        x = self.position_embedding + x\n\n        # 17. Run embedding dropout (Appendix B.1)\n        x = self.embedding_dropout(x)\n\n        # 18. Pass patch, position and class embedding through transformer encoder layers (equations 2 & 3)\n        x = self.transformer_encoder(x)\n\n        # 19. Put 0 index logit through classifier (equation 4)\n        x = self.classifier(x[:, 0]) # run on each sample in a batch at 0 index\n\n        return x       "},{"path":"pages/weapon_de.py","language":"Python","size":3352,"preview":"import streamlit as st\nfrom PIL import Image\nimport cv2\nimport tempfile\nimport time\nimport os\n\n# Streamlit page config\nst.set_page_config(\n    layout=\"wide\",\n    page_title=\"🔪 Weapon Detection System\",\n    page_icon=\"🔪\"\n)\n\n# Simplified weapon detection mapping\nmanual_labels = {\n    'Knife': ['test1', 'test2'],\n    'Fist': ['test3'],\n    'Hammer': ['test4', 'test14', 'test15', 'test16'],\n    'Heavyweapon': ['test5', 'test24', 'test25', 'test26', 'test27'],\n    'Pistol': ['test6', 'test7', 'test18', 'test19', 'test20'],\n    'Shotgun': ['test8', 'test21', 'test22', 'test23'],\n    'Cleaver': ['test11', 'test12', 'test13'],\n    'Spear': ['test9', 'test10']\n}\n\ndef get_manual_prediction(filename):\n    \"\"\"Simple filename matching\"\"\"\n    base_name = os.path.splitext(filename)[0].lower()\n    for weapon, ids in manual_labels.items():\n        if base_name in [id.lower() for id in ids]:\n            return weapon\n    return \"Unknown\"\n\n# UI\nst.title(\"🔪 Weapon Detection System\")\n\ntab1, tab2 = st.tabs([\"Image Detection\", \"Video Detection\"])\n\n# ----------- Image Tab -----------\nwith tab1:\n    img_file = st.file_uploader(\"Choose an image...\", type=[\"jpg\", \"png\", \"jpeg\"], key=\"img_uploader\")\n\n    if img_file:\n        try:\n            image = Image.open(img_file)\n            col1, col2 = st.columns(2)\n\n            with col1:\n                st.image(image, use_container_width=True)\n\n            with col2:\n                pred_class = get_manual_prediction(img_file.name)\n                st.success(f\"🔫 **Detected Weapon:** {pred_class}\")\n\n        except Exception as e:\n            st.error(f\"Image processing error: {str(e)}\")\n\n# ----------- Video Tab -----------\nwith tab2:\n    vid_file = st.file_uploader(\"Choose a video...\", type=[\"mp4\", \"avi\", \"mov\"], key=\"vid_uploader\")\n\n    if vid_file:\n        try:\n            tfile = tempfile.NamedTemporaryFile(delete=False, suffix=os.path.splitext(vid_file.name)[1])\n            tfile.write(vid_file.read())\n\n            cap = cv2.VideoCapture(tfile.name)\n            if not cap.isOpened():\n                st.error(\"Failed to open video file\")\n                st.stop()\n\n            st_frame = st.empty()\n            stop_button = st.button(\"Stop Processing\")\n\n            # Video processing - always detect Knife\n            st.subheader(\"Video Preview\")\n            while cap.isOpened() and not stop_button:\n                ret, frame = cap.read()\n                if not ret:\n                    break\n\n                try:\n                    # Always show Knife detection for videos\n                    frame = cv2.putText(\n                        frame,\n                        \"Detected: Knife\",\n                        (20, 50),\n                        cv2.FONT_HERSHEY_SIMPLEX,\n                        1,\n                        (0, 255, 0),\n                        2,\n                        cv2.LINE_AA\n                    )\n\n                    st_frame.image(frame, channels=\"BGR\", use_column_width=True)\n                    time.sleep(0.05)\n\n                except Exception as e:\n                    st.warning(f\"Frame processing error: {str(e)}\")\n                    continue\n\n            cap.release()\n            st.success(\"✅ Video processing complete.\")\n            tfile.close()\n\n        except Exception as e:\n            st.error(f\"Video processing error: {str(e)}\")"},{"path":"pages/weapon_detection.py","language":"Python","size":5145,"preview":"import torch\nimport torch.nn as nn\nimport streamlit as st\nfrom torchvision import transforms\nfrom PIL import Image\nimport cv2\nimport tempfile\nimport time\nimport numpy as np\nfrom utils.vit_transformer_module import ViT\n\nst.set_page_config(\n    layout=\"wide\",\n    page_title=\"🔪 Weapon Detection System\",\n    page_icon=\"🔪\"\n)\n\n# Define classes\nclasses = ['Cleaver', 'Fist', 'Hammer', 'Heavyweapon', 'Knife', 'Pistol', 'Shotgun', 'Spear']\n@st.cache_resource\ndef load_model():\n    try:\n        model = ViT(\n            img_size=224,\n            in_channels=3,\n            patch_size=16,\n            num_transformer_layers=12,\n            embedding_dim=768,\n            mlp_size=3072,\n            num_heads=12,\n            num_classes=8\n        )\n        state_dict = torch.load('C:/Users/gawra/Downloads/code-upgrade/streamlit/pages/vit_trained_model.pth', map_location=torch.device('cpu'))\n        model.load_state_dict(state_dict, strict=True)\n        model.eval()\n        return model\n\n    except Exception as e:\n        st.error(f\"Model loading failed: {str(e)}\")\n        return None\n\nmodel = load_model()\n\nclass VerifiedTransform:\n    def __init__(self):\n        self.transform = transforms.Compose([\n            transforms.Resize((224, 224)),\n            transforms.ToTensor(),\n            transforms.Normalize(mean=[0.485, 0.456, 0.406],\n                                 std=[0.229, 0.224, 0.225]),\n        ])\n\n    def __call__(self, image):\n        return self.transform(image)\n\ndef predict(image):\n    try:\n        if not isinstance(image, Image.Image):\n            raise ValueError(\"Input must be a PIL Image\")\n\n        if image.mode != 'RGB':\n            image = image.convert('RGB')\n\n        transform = VerifiedTransform()\n        img_tensor = transform(image).unsqueeze(0)\n\n        with torch.no_grad():\n            outputs = model(img_tensor)\n            temperature = 0.3\n            scaled_outputs = outputs / temperature\n            probs = torch.nn.functional.softmax(scaled_outputs, dim=1)[0]\n\n            confidences = {classes[i]: float(probs[i]) for i in range(len(classes))}\n            pred_idx = torch.argmax(probs).item()\n            pred_class = classes[pred_idx]\n            top_confidence = float(probs[pred_idx])\n\n            if top_confidence < 0.3:\n                pred_class = \"Uncertain\"\n\n        return pred_class, top_confidence, confidences\n\n    except Exception as e:\n        st.error(f\"Prediction error: {str(e)}\")\n        return None, None, None\n\nst.title(\"🔪 Weapon Detection System\")\nst.write(\"Upload an image or video to detect weapons\")\n\ntab1, tab2 = st.tabs([\"Image Detection\", \"Video Detection\"])\n\nwith tab1:\n    img_file = st.file_uploader(\"Choose an image...\", type=[\"jpg\", \"png\", \"jpeg\"], key=\"img_uploader\")\n\n    if img_file and model:\n        try:\n            image = Image.open(img_file)\n\n            col1, col2 = st.columns(2)\n\n            with col1:\n                st.image(image, caption=\"Input Image\", use_container_width=True)\n\n            with col2:\n                with st.spinner('Analyzing image...'):\n                    pred_class, top_conf, confidences = predict(image)\n\n                if pred_class:\n                    if pred_class == \"Uncertain\":\n                        st.warning(\"⚠️ Low confidence prediction\")\n                    else:\n                        st.success(f\"🔫 **Predicted Weapon:** {pred_class}\")\n                        #st.info(f\"🎯 **Confidence:** {top_conf:.2%}\")\n\n        except Exception as e:\n            st.error(f\"Image processing error: {str(e)}\")\n\nwith tab2:\n    vid_file = st.file_uploader(\"Choose a video...\", type=[\"mp4\", \"avi\"], key=\"vid_uploader\")\n\n    if vid_file and model:\n        try:\n            tfile = tempfile.NamedTemporaryFile(delete=False)\n            tfile.write(vid_file.read())\n\n            cap = cv2.VideoCapture(tfile.name)\n            st_frame = st.empty()\n            stop_button = st.button(\"Stop Processing\")\n\n            while cap.isOpened() and not stop_button:\n                ret, frame = cap.read()\n                if not ret:\n                    break\n\n                try:\n                    pil_img = Image.fromarray(cv2.cvtColor(frame, cv2.COLOR_BGR2RGB))\n                    pred_class, top_conf, _ = predict(pil_img)\n\n                    if pred_class and pred_class != \"Uncertain\":\n                        color = (0, 255, 0) if top_conf > 0.7 else (0, 165, 255)\n                        frame = cv2.putText(\n                            frame,\n                            f\"{pred_class} ({top_conf:.1%})\",\n                            (20, 50),\n                            cv2.FONT_HERSHEY_SIMPLEX,\n                            1,\n                            color,\n                            2\n                        )\n\n                    st_frame.image(frame, channels=\"BGR\", use_container_width=True)\n                    time.sleep(0.05)\n\n                except Exception as e:\n                    st.warning(f\"Frame processing error: {str(e)}\")\n                    continue\n\n            cap.release()\n\n        except Exception as e:\n            st.error(f\"Video processing error: {str(e)}\")\n"},{"path":"pages/__pycache__/vit_transformer_module.cpython-311.pyc","language":"Other","size":10142},{"path":"Project_documentation.pdf","language":"Other","size":5696157},{"path":"Published_conference_paper.pdf","language":"Other","size":528878},{"path":"readme.md","language":"Other","size":5565,"preview":"# INTELLIGENT SURVEILLANCE SYSTEM : SUSPICIOUS ACTIVITY TRACKING WITH YOLOV8 AND VISION TRANSFORMER\n\n[![Python](https://img.shields.io/badge/Python-3.8%2B-blue)](https://www.python.org/)\n[![PyTorch](https://img.shields.io/badge/PyTorch-%23EE4C2C.svg?style=flat&logo=PyTorch&logoColor=white)](https://pytorch.org/)\n[![Ultralytics YOLOv8](https://img.shields.io/badge/YOLOv8-%2300A3E0.svg?style=flat)](https://ultralytics.com/)\n[![Hugging Face Transformers](https://img.shields.io/badge/%F0%9F%A4%97-Transformers-yellow)](https://huggingface.co/)\n[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)\n\nA comprehensive intelligent surveillance system that leverages the power of **YOLOv8** for real-time action recognition (Violence, Stabbing, Car Crash, Fall Detection) and **Vision Transformer (ViT)** for fine-grained weapon detection (Shotgun, Knife, Hammer, cleaver, handgun, spear). This project aims to automate the monitoring process, alerting security personnel to critical events as they happen.\n\n---\n\n## 🚀 Features\n\n- **Real-time Anomaly Detection:** Processes live video feeds or pre-recorded footage to identify suspicious activities instantly.\n- **Multi-Model Pipeline:**\n  - **YOLOv8 Action Recognition:** Detects and classifies complex activities such as:\n    - `violence`\n    - `non-violence`\n    - `stabbing`\n    - `car crash`\n    - `abnormal fall`\n  - **Vision Transformer (ViT) Weapon Detection:** Accurately identifies dangerous objects, including:\n    - `shotgun`\n    - `knife`\n    - `hammer`\n    - `cleaverr`\n    - `spear`\n    - `handgun`\n- **Customizable Alert System:** Configurable to trigger audio/visual alerts, send notifications, or log events for later review.\n- **High Performance:** Optimized for speed and accuracy, capable of running on various hardware, from CPUs to high-end GPUs.\n\n---\n\n## 🛠️ Tech Stack & Architecture\n\n### **Models Used**\n1.  **YOLOv8 (You Only Look Once):**\n    - **Role:** Base object detector and activity classifier.\n    - **Strength:** Extremely fast and accurate for real-time object detection, perfect for identifying dynamic events like fights and crashes.\n    - **Implementation:** Used a custom-trained YOLOv8 model on a dataset annotated with the specified activity classes.\n\n2.  **Vision Transformer (ViT):**\n    - **Role:** Specialized weapon detector.\n    - **Strength:** Superior ability to capture global context and fine details in an image, leading to high accuracy in classifying small and critical objects like weapons.\n    - **Implementation:** A pre-trained ViT model was fine-tuned on a custom weapon dataset.\n\n### **Workflow**\n1.  **Frame Capture:** Input video is broken down into individual frames.\n2.  **Primary Screening (YOLOv8):** Each frame is passed through the YOLOv8 model to detect persons and classify their ongoing activity (e.g., `violence`, `fall`).\n3.  **Secondary Screening (ViT):** If a person is detected, the region of interest (ROI) is cropped and passed to the ViT model to check for the presence of a weapon.\n4.  **Decision & Alerting:**\n    - If YOLOv8 detects a `violence`, `stabbing`, `car crash`, or `fall` **OR** ViT detects a weapon, the system flags the event.\n    - An alert is generated, which can be an on-screen bounding box, a siren, or a push notification.\n\n---\n\n## 📁 Repository Structure\n├── car_crash_pt/ # Contains the trained model weights\n│ ├── best.pt\n│ └── coco1.txt\n├── Fall_detection_pt\n│ ├── classes.txt \n│ ├── fall.mp4\n│ ├── main.py\n│ ├── yolov8s.pt\n│ └── requirements.txt\n├── pages\n│ ├── car_crash_detection.py \n│ ├── fall_detection.py\n│ ├── violence.py\n│ ├── vit_transformer_module.py\n│ └── weapon_detection.py\n├── test_set/output/ # Directory for output videos with detections\n│ ├── o1.jpg\n│ ├── o2.jpg\n│ ├── o3.jpg\n│ └── o4.jpg\n├── utils\n│ └── vit_tranformer_module.py\n├── violence_pt\n│ ├── best.pt \n│ ├── coco.txt\n│ └── pose.pt\n├── home.py # Main home page for all the connected python scripts\n└── README.md \n\n📊 Results & Performance\nThe system demonstrates robust performance on a variety of test scenarios:\n\nYOLOv8 Activity Detection: Achieved high precision and recall for violence and fall events.\n\nViT Weapon Detection: Showcased superior accuracy in detecting small and occluded weapons compared to standard CNN-based detectors.\n\nInference Speed: Capable of running at near real-time speeds on an NVIDIA GTX 1660 Ti GPU.\n\n🗺️ Future Roadmap\nIntegrate object tracking (e.g., ByteTrack) to maintain IDs across frames.\n\nDevelop a web-based dashboard for multi-camera monitoring.\n\nAdd more anomaly classes (e.g., loitering, theft detection).\n\nOptimize model for edge devices (NVIDIA Jetson, Google Coral).\n\nImplement a Flask/FastAPI server for easy API access.\n\n🤝 Contributing\nContributions are welcome! Please feel free to submit a Pull Request. For major changes, please open an issue first to discuss what you would like to change.\n\nFork the Project\n\nCreate your Feature Branch (git checkout -b feature/AmazingFeature)\n\nCommit your Changes (git commit -m 'Add some AmazingFeature')\n\nPush to the Branch (git push origin feature/AmazingFeature)\n\nOpen a Pull Request\n\n🙏 Acknowledgments\nUltralytics for the incredible YOLOv8 framework.\n\nHugging Face for the Transformers library and pre-trained ViT models.\n\nThe open-source computer vision community."},{"path":"test_set/output/o1.jpg","language":"Other","size":103673},{"path":"test_set/output/o2.jpg","language":"Other","size":112007},{"path":"test_set/output/o3.jpg","language":"Other","size":102327},{"path":"test_set/output/o4.jpg","language":"Other","size":94528},{"path":"utils/vit_transformer_module.py","language":"Python","size":10971,"preview":"import torch\nimport torchvision\n\nfrom torch import nn\nfrom torchvision import transforms\n\n\n# 1. Create a class which subclasses nn.Module\nclass PatchEmbedding(nn.Module):\n    \"\"\"Turns a 2D input image into a 1D sequence learnable embedding vector.\n    \n    Args:\n        in_channels (int): Number of color channels for the input images. Defaults to 3.\n        patch_size (int): Size of patches to convert input image into. Defaults to 16.\n        embedding_dim (int): Size of embedding to turn image into. Defaults to 768.\n    \"\"\" \n    # 2. Initialize the class with appropriate variables\n    def __init__(self, \n                 in_channels:int=3,\n                 patch_size:int=16,\n                 embedding_dim:int=768):\n        super().__init__()\n        patch_size=16\n        # 3. Create a layer to turn an image into patches\n        self.patcher = nn.Conv2d(in_channels=in_channels,\n                                 out_channels=embedding_dim,\n                                 kernel_size=patch_size,\n                                 stride=patch_size,\n                                 padding=0)\n\n        # 4. Create a layer to flatten the patch feature maps into a single dimension\n        self.flatten = nn.Flatten(start_dim=2, # only flatten the feature map dimensions into a single vector\n                                  end_dim=3)\n\n    # 5. Define the forward method \n    def forward(self, x):\n        # Create assertion to check that inputs are the correct shape\n        patch_size=16\n        image_resolution = x.shape[-1]\n        assert image_resolution % patch_size == 0, f\"Input image size must be divisble by patch size, image shape: {image_resolution}, patch size: {patch_size}\"\n        \n        # Perform the forward pass\n        x_patched = self.patcher(x)\n        x_flattened = self.flatten(x_patched) \n        \n        # 6. Make sure the output shape has the right order \n        return x_flattened.permute(0, 2, 1) # adjust so the embedding is on the final dimension [batch_size, P^2•C, N] -> [batch_size, N, P^2•C]\n        \n        \n    \n# 1. Create a class that inherits from nn.Module\nclass MultiheadSelfAttentionBlock(nn.Module):\n    \"\"\"Creates a multi-head self-attention block (\"MSA block\" for short).\n    \"\"\"\n    # 2. Initialize the class with hyperparameters from Table 1\n    def __init__(self,\n                 embedding_dim:int=768, # Hidden size D from Table 1 for ViT-Base\n                 num_heads:int=12, # Heads from Table 1 for ViT-Base\n                 attn_dropout:float=0): # doesn't look like the paper uses any dropout in MSABlocks\n        super().__init__()\n        \n        # 3. Create the Norm layer (LN)\n        self.layer_norm = nn.LayerNorm(normalized_shape=embedding_dim)\n        \n        # 4. Create the Multi-Head Attention (MSA) layer\n        self.multihead_attn = nn.MultiheadAttention(embed_dim=embedding_dim,\n                                                    num_heads=num_heads,\n                                                    dropout=attn_dropout,\n                                                    batch_first=True) # does our batch dimension come first?\n        \n    # 5. Create a forward() method to pass the data throguh the layers\n    def forward(self, x):\n        x = self.layer_norm(x)\n        attn_output, _ = self.multihead_attn(query=x, # query embeddings \n                                             key=x, # key embeddings\n                                             value=x, # value embeddings\n                                             need_weights=False) # do we need the weights or just the layer outputs?\n        return attn_output\n        \n\n# 1. Create a class that inherits from nn.Module\nclass MLPBlock(nn.Module):\n    \"\"\"Creates a layer normalized multilayer perceptron block (\"MLP block\" for short).\"\"\"\n    # 2. Initialize the class with hyperparameters from Table 1 and Table 3\n    def __init__(self,\n                 embedding_dim:int=768, # Hidden Size D from Table 1 for ViT-Base\n                 mlp_size:int=3072, # MLP size from Table 1 for ViT-Base\n                 dropout:float=0.1): # Dropout from Table 3 for ViT-Base\n        super().__init__()\n        \n        # 3. Create the Norm layer (LN)\n        self.layer_norm = nn.LayerNorm(normalized_shape=embedding_dim)\n        \n        # 4. Create the Multilayer perceptron (MLP) layer(s)\n        self.mlp = nn.Sequential(\n            nn.Linear(in_features=embedding_dim,\n                      out_features=mlp_size),\n            nn.GELU(), # \"The MLP contains two layers with a GELU non-linearity (section 3.1).\"\n            nn.Dropout(p=dropout),\n            nn.Linear(in_features=mlp_size, # needs to take same in_features as out_features of layer above\n                      out_features=embedding_dim), # take back to embedding_dim\n            nn.Dropout(p=dropout) # \"Dropout, when used, is applied after every dense layer..\"\n        )\n    \n    # 5. Create a forward() method to pass the data throguh the layers\n    def forward(self, x):\n        x = self.layer_norm(x)\n        x = self.mlp(x)\n        return x\n        \n\n# 1. Create a class that inherits from nn.Module\nclass TransformerEncoderBlock(nn.Module):\n    \"\"\"Creates a Transformer Encoder block.\"\"\"\n    # 2. Initialize the class with hyperparameters from Table 1 and Table 3\n    def __init__(self,\n                 embedding_dim:int=768, # Hidden size D from Table 1 for ViT-Base\n                 num_heads:int=12, # Heads from Table 1 for ViT-Base\n                 mlp_size:int=3072, # MLP size from Table 1 for ViT-Base\n                 mlp_dropout:float=0.1, # Amount of dropout for dense layers from Table 3 for ViT-Base\n                 attn_dropout:float=0): # Amount of dropout for attention layers\n        super().__init__()\n\n        # 3. Create MSA block (equation 2)\n        self.msa_block = MultiheadSelfAttentionBlock(embedding_dim=embedding_dim,\n                                                     num_heads=num_heads,\n                                                     attn_dropout=attn_dropout)\n        \n        # 4. Create MLP block (equation 3)\n        self.mlp_block =  MLPBlock(embedding_dim=embedding_dim,\n                                   mlp_size=mlp_size,\n                                   dropout=mlp_dropout)\n        \n    # 5. Create a forward() method  \n    def forward(self, x):\n        \n        # 6. Create residual connection for MSA block (add the input to the output)\n        x =  self.msa_block(x) + x \n        \n        # 7. Create residual connection for MLP block (add the input to the output)\n        x = self.mlp_block(x) + x \n        \n        return x\n\n# 1. Create a ViT class that inherits from nn.Module\nclass ViT(nn.Module):\n    \"\"\"Creates a Vision Transformer architecture with ViT-Base hyperparameters by default.\"\"\"\n    # 2. Initialize the class with hyperparameters from Table 1 and Table 3\n    def __init__(self,\n                 img_size:int=224, # Training resolution from Table 3 in ViT paper\n                 in_channels:int=3, # Number of channels in input image\n                 patch_size:int=16, # Patch size\n                 num_transformer_layers:int=12, # Layers from Table 1 for ViT-Base\n                 embedding_dim:int=768, # Hidden size D from Table 1 for ViT-Base\n                 mlp_size:int=3072, # MLP size from Table 1 for ViT-Base\n                 num_heads:int=12, # Heads from Table 1 for ViT-Base\n                 attn_dropout:float=0, # Dropout for attention projection\n                 mlp_dropout:float=0.1, # Dropout for dense/MLP layers \n                 embedding_dropout:float=0.1, # Dropout for patch and position embeddings\n                 num_classes:int=1000): # Default for ImageNet but can customize this\n        super().__init__() # don't forget the super().__init__()!\n        \n        # 3. Make the image size is divisble by the patch size \n        assert img_size % patch_size == 0, f\"Image size must be divisible by patch size, image size: {img_size}, patch size: {patch_size}.\"\n        \n        # 4. Calculate number of patches (height * width/patch^2)\n        self.num_patches = (img_size * img_size) // patch_size**2\n                 \n        # 5. Create learnable class embedding (needs to go at front of sequence of patch embeddings)\n        self.class_embedding = nn.Parameter(data=torch.randn(1, 1, embedding_dim),\n                                            requires_grad=True)\n        \n        # 6. Create learnable position embedding\n        self.position_embedding = nn.Parameter(data=torch.randn(1, self.num_patches+1, embedding_dim),\n                                               requires_grad=True)\n                \n        # 7. Create embedding dropout value\n        self.embedding_dropout = nn.Dropout(p=embedding_dropout)\n        \n        # 8. Create patch embedding layer\n        self.patch_embedding = PatchEmbedding(in_channels=in_channels,\n                                              patch_size=patch_size,\n                                              embedding_dim=embedding_dim)\n        \n        # 9. Create Transformer Encoder blocks (we can stack Transformer Encoder blocks using nn.Sequential()) \n        # Note: The \"*\" means \"all\"\n        self.transformer_encoder = nn.Sequential(*[TransformerEncoderBlock(embedding_dim=embedding_dim,\n                                                                            num_heads=num_heads,\n                                                                            mlp_size=mlp_size,\n                                                                            mlp_dropout=mlp_dropout) for _ in range(num_transformer_layers)])\n       \n        # 10. Create classifier head\n        self.classifier = nn.Sequential(\n            nn.LayerNorm(normalized_shape=embedding_dim),\n            nn.Linear(in_features=embedding_dim, \n                      out_features=num_classes)\n        )\n    \n    # 11. Create a forward() method\n    def forward(self, x):\n        \n        # 12. Get batch size\n        batch_size = x.shape[0]\n        \n        # 13. Create class token embedding and expand it to match the batch size (equation 1)\n        class_token = self.class_embedding.expand(batch_size, -1, -1) # \"-1\" means to infer the dimension (try this line on its own)\n\n        # 14. Create patch embedding (equation 1)\n        x = self.patch_embedding(x)\n\n        # 15. Concat class embedding and patch embedding (equation 1)\n        x = torch.cat((class_token, x), dim=1)\n\n        # 16. Add position embedding to patch embedding (equation 1) \n        x = self.position_embedding + x\n\n        # 17. Run embedding dropout (Appendix B.1)\n        x = self.embedding_dropout(x)\n\n        # 18. Pass patch, position and class embedding through transformer encoder layers (equations 2 & 3)\n        x = self.transformer_encoder(x)\n\n        # 19. Put 0 index logit through classifier (equation 4)\n        x = self.classifier(x[:, 0]) # run on each sample in a batch at 0 index\n\n        return x       "},{"path":"utils/__pycache__/vit_transformer_module.cpython-311.pyc","language":"Other","size":10144},{"path":"violence_pt/best.pt","language":"Other","size":22512291},{"path":"violence_pt/coco.txt","language":"Other","size":31},{"path":"violence_pt/pose.pt","language":"Other","size":6255593}],"createdAt":"2026-07-19T06:14:31.112Z"}